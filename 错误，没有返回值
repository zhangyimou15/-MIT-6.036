import numpy as np
def score(data, labels, th, th0):
    np.sum(labels==positive(data,th,th0),axis=1)


正确的：：
import numpy as np
def score(data, labels, th, th0):
   return np.sum(positive(data, th, th0) == labels)
